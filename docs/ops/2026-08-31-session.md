# 2026-08-31 セッション日誌

## hunk活用の拡張（hunk-review skillの全CLI配線）

- hunk同梱の `hunk-review` skill を `scripts/link` で全エージェントCLIへ
  symlinkした(`f9c11dd`)。リンク先は `$(brew --prefix hunk)` 配下なので、
  brew upgrade でバージョンが変わってもリンクは切れない。
- `agents/global-instructions.md` にルーティング規範を1箇条追記した。
  live session があるrepoではdiff解説をhunkコメント+navigateで届け、
  chatには要約だけを残す。Plannotatorは永続的なguided review、hunkは
  session限りのlive view。役割はここで分けた。
- 見送りはSTMLマークアップ・hunk extensions・`--agent-context` sidecar
  の3つ。いずれもYAGNI。STMLはexperimentalで `--experimental` 起動時
  しか効かない。必要が出てから足す。
- 検証: shellcheck通過。`scripts/link` 実行で4箇所のsymlink作成と、
  live daemonへの `hunk session list` 応答を確認した。

# 2026-09-02 セッション日誌

## terminal-browser の導入確認と性能改善

- terminal-browser skill は本体同梱(`~/.local/share/terminal-browser/app/skills/`)で
  `.agents`/`.claude`/`.codex` へ配線済みを確認。`.copilot` のみ未配線(必要になったら
  symlink 1本)。symlink はツールのインストーラ管理で `scripts/link` の対象外。
- 「もっさりする」報告への対処: v0.5.8 → v0.7.6 へ `terminal-browser upgrade`。
  8/21 から残っていた旧プロセス(`~/.local/lib/tode/vendor/` 同梱の古いコピー)も kill。
- 軽く使う運用: `--split right --size 0.4` 程度でペインを小さく、重いサイトの解析は
  headless の内蔵 browser ツール、使用後は `shutdown`。
- 実用デモ: caps-app.jp の性能計測(LCP 10.9s — hero 画像の preload/sizes 不一致による
  二重フェッチが主因、フォント 48 ファイル 918KB、トラッカー長尾)。Google 検索表示も動作確認。

## ブラウザ検証経路の整理(browser-verify skill)

- terminal-browser のもっさり感を受けて経路を再決定。人が見る必要があるかで選ぶ:
  エージェントだけ→headless(OMP `browser` ツール)、人が見る→使い捨て `--user-data-dir` の
  別窓 Chrome(リテラル `--guest` は自動化に制約があるので不採用)、ログイン済み→relay、
  terminal-browser は「ペインの横で見せて」と明示された時だけ。
- 別窓 Chrome で Google 検索「ひがふな」を実演し、滑らかさを確認して `kill` で閉じた。
- 手順を `agents/skills/browser-verify/SKILL.md` に集約(1スキル+モード別セクション、
  OMP / Claude Code / Codex 別レシピ)。`global-instructions.md` の節は判断ルールと
  スキルへのポインタだけに縮めた。
- headless は OMP 専用と割り切り、Claude Code / Codex は claude-in-chrome / Codex browser
  plugin に落とす。agent-browser 等の追加導入はしない(ユーザー決定)。

## ツール一括更新(scripts/brewUpdate)

- 13 パッケージ更新: codex 0.152.0、claude-code 2.1.252、orca 1.4.194、
  sonarqube-cli 1.7.0、kiro-cli、openssl@3、pcre2、Slack/Discord/Notion/TablePlus/WhatsApp。
  herdr skill 再生成済み。omp 本体は最新のまま。

## 宿題

1. karabiner-elements 16.2.0 への更新が sudo 非対話で失敗。手元のターミナルで
   `brew upgrade --cask karabiner-elements` を実行(パスワード入力あり)。
2. 上記失敗により `brew cleanup` がスキップされたまま。karabiner 更新後に
   `scripts/brewUpdate` を再実行すると cleanup まで完走する。
3. stash 2件の整理が未着手のまま継続
   (`omp config local changes before pull`、`pre-rebase stash: claude/settings.json local edit`)。
4. `.copilot/skills/terminal-browser` の symlink は Copilot CLI で使いたくなったら追加。

## omp-herdr-collab の learn 候補保存（別セッション）

### 完了

- Warpのself-improving agentパターンを既存の改善サイクルと比較した。
- `omp-herdr-collab`のレビュー完了処理へ、検証済みで再利用可能な指摘だけを`learn`の候補として保存する手順を追加した。
- 未検証の指摘、通常のコードバグ、単発の好みは保存せず、skillへの昇格は`omp-learning-loop`の裏取りと項目別承認に委ねる設計とした。
- 変更は`f35f5ef`として`origin/main`へpushした。
- 1Passwordのアンロック後に`git pull --ff-only`を再実行し、`f35f5ef..04efe4e`をfast-forwardで取り込んだ。`main`と`origin/main`は同期している。

### 未完了

- なし。

この日誌は引き継ぎ用であり、自動コミットしない。

# 2026-08-30 セッション日誌

## hunk の導入と herdr-collab への組み込み

- `dotfiles/.Brewfile` に `brew "hunk"`(0.20.1)を追加した(`8b33c41`)。
  スモークテスト済み。live TUI を hub プロセスで起動し、別プロセスから
  `hunk session comment add` の往復まで確認した。
- `herdr-collab` に「hunk による live 案内(任意・presentation 層)」の節を
  追加した(`58f547d`)。契約は不変。台帳が唯一の正本で、hunk コメントは
  session 限りの表示、操作は coordinator のみ。

## docs と実装の乖離監査

scout 4並列で README / docs / omp / agents を実装と突合した。実装は正しく
動いていたので、triage の上でドキュメント側だけを直した。

- `47a7b15`: 6ファイルを修正。guard の手動優先記述の是正、ponytail の
  plugin 一覧追加、ponytail cache の共有記述撤回、jbcontext 予防フラグ手順の
  検知ベース化、gh 拡張の条件付き導入、sonar token 再生成、plist の render
  配置。
- `2d3c37b`: codex クロスレビュー(flow: docs-align、artifact-import)の
  FINDINGS 2件(mid)を反映した。ひとつは guard の例外。両プール98%枯渇時の
  local rescue は、手動選択の後でももう一度だけ上書きする。もうひとつは
  new-machine.md に残っていた機械固有の導入元の削除。`require-closed` は
  closed-pass(台帳: `.agent-msgs/docs-align/`)。
- 棄却した指摘: `.worktreeinclude` 不在(規約記述であり、helper は exit 0
  する設計)、diagram-workflow の例示パス、plannotator plugin 配布で実在する
  reviewable-design-doc / visual-html-renderer、setup 冪等主張。
- 残課題だった `gh` の所有レイヤーは devbox global に決定(ユーザー判断)。
  `scripts/devbox` の packages へ追加して実行し(gh 2.98.0 導入)、
  `nix profile remove gh` で素の nix 導入を撤去。`which gh` が devbox
  profile を指し、`gh auth status` も keyring 認証のまま通ることを確認。
- 続けて nix profile の残り7パッケージも整理し、profile は空になった。
  現役だった awscli2 / github-copilot-cli は devbox global へ移行
  (`scripts/devbox` に追加)。php / xdebug / cyclonedx-php-composer /
  python3 / bash-interactive は PATH 最下位の nix profile で既に devbox・
  uv 版に shadow されていた死蔵のため削除のみ。`which` で全コマンドが
  devbox / `~/.local/bin` を指し、`aws --version`・`copilot --version`
  の動作を確認。

Sonar Quality Gate: PASSED(47a7b15 時点、2d3c37b は docs のみ)。

## herdr-collab の再編(omp-herdr-collab へ)

実運用証拠(docs-align 台帳、日誌、git 履歴)から課題を洗い、方向性を
項目別承認のうえ全面再編した。coordinator は omp セッションに固定し、
Claude Code / Codex は peer としてテンプレ経由で契約を受ける。

- `524d868`: `herdr-collab` -> `omp-herdr-collab` へ改名。sonar パス追随。
- `7e897d4`: validator の実装改修。REVIEW-REQ への handoff フィールド統合
  (go/no-go 往復を1つ削減)、`scaffold`(REVIEW-REQ から FINDINGS/VERIFIED
  skeleton を生成し scope 逐語コピー拒否を根絶)、`status` の次の一手表示、
  snapshot revision 廃止(実走ゼロ・digest 手段なし、commit 固定に一本化)、
  `send.sh --help` をフラグと exit code の正本化。44 テスト OK。
- `7ae974b`: SKILL.md を omp 前提で再編・圧縮(582行 -> 約300行)。実走ゼロの
  panel(本文の約4割)を `omp-herdr-collab-panel` へ抽出し明示時のみロード。
  peer への契約は同梱 `templates/`(reviewer 指示・briefing 雛形)が正本。
  briefing に acceptance criteria・非目標・focus catalog・検証手段を必須化。
- `514d968`: global-instructions / README / adversarial-verification /
  github-pr-review を新役割へ追随。agent-usage-review の診断表に非自明 PR の
  cross-review 経由率を追加(使い忘れの週次計測)。
- `scripts/link` 実行済み。旧 `herdr-collab` symlink は 4 CLI とも retire、
  新 skill 2 つがリンクされたことを確認。
- 回帰検証: 旧台帳 `docs-align` が `require-closed` で closed-pass のまま、
  scaffold の逐語一致、handoff 統合 REVIEW-REQ の受理と snapshot 拒否を
  スモーク確認。
- sonar 指摘 2 件(S1192/S3776)を修正し、CLI 統合テスト形式で coverage が
  プロセス境界を越えられないため collab scripts のみ coverage 免除
  (`06ff2fe`、issue 検出は維持)。

Sonar Quality Gate: PASSED(再編後)。

## omp-herdr-collab-panel のセルフレビューと修正

- 矛盾点(high): panel の peer 契約が配達されない。single 用テンプレは
  数値 ID 前提で、panel validator の名前空間 ID(`finding-a-N`)・
  `reviewer`/`lens` 欄・CROSS-CHECK 形式・VERIFIED の `finding-ids` を
  満たせず、旧 skill で実測した edit-retry ループの再発経路だった。
- `e98f0c2`: validator に `scaffold --tag cross-check` を実装(peer の
  high/mid を台帳から導出。independence barrier のため両 FINDINGS 記録後
  のみ)。panel VERIFIED skeleton へ `finding-ids` を追加し、CONSOLIDATED
  記録後は所有 canonical ID を自動 prefill、zero-ID は partition も none で
  埋める。テスト 44->47 件、OK。
- `6f0e5f7`: self-contained な `reviewer-instructions-panel.md` を追加。
  panel SKILL へ scaffold 実行タイミング、`instructions:` の指定、
  reviewer-b spawn 既定名の実装者との衝突注意、CROSS-CHECK の evidence
  prefix が peer 側であることを明記。
- 見送り: CONSOLIDATED の scaffold 化(coordinator = skill 読者の作業で、
  判断を伴う triage のため機械生成の価値が薄い)。

Sonar Quality Gate: PASSED(panel 修正後)。

## Semgrep quality gate の machine-global 導入

SonarQube と併用する Semgrep を、sonar と同型の opt-in gate として導入した。
役割分担: sonar は履歴つきの品質判定、semgrep は serverless の高速 pattern
検査と repository 固有 rule。

- `3b36515`: `scripts/semgrep-quality-gate` を新設。root の `.semgrep.yaml`
  を opt-in file とし、無ければ skip(exit 0)、findings は exit 1。
  `--metrics=off` で解析を local に保つ。semgrep は devbox global へ追加
  (`319f091`)。
- `ef28de7`: global-instructions の Repository quality gates を両 gate の
  対応表に拡張し、README・`docs/semgrep.md` を整備。dotfiles 自身の
  `.semgrep.yaml` に scripts/link 76ec771 由来の pipefail+`grep -q` rule を
  収録した。
- codex クロスレビュー(flow: semgrep-gate、artifact-import)。FINDINGS
  2件(mid)を反映(`8a425b5`): rule の pipefail 前提条件追加と、registry
  ruleset の `>>` 追記手順の廃止(YAML の top-level `rules:` が重複する)。
  VERIFIED で pipefail 判定の文字列ヒューリスティック(コメント言及・
  `set +o pipefail` も検知)が unresolved-mid となり、ユーザー DECISION は
  rework。
- rework(flow: semgrep-gate-2、`1fa4fd4`): pattern-either で宣言形
  (`set -o pipefail` / `set -euo pipefail`)だけを前提条件にし、6 fixture
  (宣言2形・コメントのみ・明示無効・未記載・printf)で挙動を確認。
  再レビューは count: 0 で `require-closed` は closed-pass。

両 Quality Gate: PASSED(semgrep 52 files / 0 findings、sonar は ef28de7
時点で PASSED、以後の変更は sonar.sources 外)。

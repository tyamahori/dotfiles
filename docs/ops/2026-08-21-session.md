# セッション日誌 2026-08-21（開始 2026-08-20、日次手仕舞い）

## 完了（push 済み、`14991f8..7f3353b`）

- `ffdb726` `scripts/devbox` に `jujutsu`、`ccusage`、`jq`、`sqlite` を追加。
  Devbox global へ導入し、`jj 0.44.0`、`ccusage 20.0.17`、`sqlite3 3.53.3` を確認。
- `7f3353b` usage review の `ccusage` 実行を `npx -y ccusage@latest` から
  Devbox 管理バイナリへ切り替え。`snapshot.sh --days 1` は stats 同期、全セクション生成、
  plugin/config drift 検査まで完走。
- ローカルの agent-usage-review journal に、計測ツール管理の適用・検証結果を記録。

## 未コミット・宿題

- `omp/config.yml` に今回の作業とは無関係な変更が残っている。
  `setupVersion: 1` から `2`、`composer.shape: pi` の追加、および行末空白の差分。
  生成元と採否は未確認。次の新規セッションで、保持・整形して commit・破棄のどれかを判断する。
- このセッション日誌自体も手仕舞い時点では未コミット。

## 引継ぎ後の解決

- OMP 17.4.0 のセットアップウィザードが `setupVersion: 2` と `composer.shape: pi` を保存した変更だった。
  現行設定として認識されているため、`omp/config.yml` に残して別コミットにする。

## 決定事項

- グローバル CLI は Devbox を正本とし、Nix profile へ重複追加しない。
- usage review の必須集計ツールは都度取得せず、Devbox 管理のローカルバイナリを使う。
- `jj` は Git 前提の代替ではないが、未導入を仮定した失敗・再試行を避けるため常備する。

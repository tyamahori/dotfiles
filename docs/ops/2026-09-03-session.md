# 2026-09-03 セッション日誌

## ハーネスの詰め（OpenHarness 検討から派生、09-02 開始・09-03 手仕舞い）

- HKUDS/OpenHarness は Claude Code 相当の Python 実装で、このマシンの
  OMP/Claude Code/Codex + extensions/hooks/skills と機能が重なる。サブスク認証の
  横流し方式と規約リスクもあり導入しない。代わりに既存ハーネスの穴を 3 点で詰めた。
- 完了: Codex の `SessionStart`（`resume|compact`）に session-hygiene フックを配線し、
  Claude Code(hook)・OMP(extension) と日跨ぎ resume / 2回目以降の compaction の警告を
  揃えた。スクリプトは `scripts/session-hygiene-hook` へ移して共有（`4378daa`、push 済み）。
- 見送り: `omp/extensions` の tsc 型チェックを pre-commit へ追加する案。素の tsc は
  `process` / `bun:sqlite` / `@oh-my-pi/pi-coding-agent` 未解決で 200 件超のノイズになり、
  `@types/node` と omp の型パッケージを repo に持ち込まないと gate にならない。
  lint-on-edit(oxlint) の範囲で十分と判断。
- 計測（snapshot 14 日、`.agent-msgs/scratch/snapshot-14d.md`、ローカル専用）:
  常駐指示は 20,244 + 3,131 bytes で cache read 率 96〜98%。skill 読込は 08-20〜22 に
  集中（agent-collab 87、coach 45、adversarial-verification 46）し、08-25 の
  agent-collab 廃止以降は natural-japanese / efficient-python が 1〜3 回/日、
  adversarial-verification が 1 回/日程度に落ち着いている。常駐指示や skill の削減は
  データが支持しないため適用なし。
- 目立った数値は compaction 131 回に対し handoff 3 回、cacheWrite の 61% が >50k 応答。
  これは 09-01 の usage review の宿題 3（compaction≥2 → handoff 転換）と同じ指標なので、
  次回 09-07 頃のレビューで扱う。deny ガードの拒否は shell-read 51 回 / bare-python 25 回
  （14 日、OMP）で、緩める価値はない。

## Claude Code 設定案の検討（未適用）

ユーザー案 `{advisorModel: "fable", permissions.defaultMode: "auto", viewMode: "focus"}` を
公式 docs（settings-reference / advisor）と `claude/settings.json` で突合した。

- `permissions.defaultMode: "auto"` は `claude/settings.json:410` に設定済みで変化なし。
- `viewMode: "focus"` は表示のみの変更（直前プロンプト・ツール 1 行要約・最終応答）。
  fullscreen renderer 前提だが settings に `tui` 指定はなく、起動確認が必要。導入は問題なし。
- `advisorModel: "fable"` は quota リスクあり: advisor は呼ばれるたびに会話全体を
  非キャッシュで再読し（200k context なら 1 回 Fable 200k input 相当）、回数上限なし、
  subagent も継承。main が `claude-fable-5-1` なので許可される advisor は Fable 5.1 のみ。
  Fable の usage-credits consent 未了なら `/model fable` で承諾しないと無効。
- 提案: `viewMode` は常設、`advisorModel` は常設せず長い refactor / debug のセッションで
  `claude --advisor fable` か `/advisor fable` を一時利用し `/usage` で消費を見てから判断。

## 宿題

1. 上記設定案の採否がユーザー未回答。適用するなら `claude/settings.json` を直接編集する
   （`update-config` skill は使わない）。
2. `omp/extensions/orca-agent-status.ts` / `orca-titlebar-spinner.ts` の作業中差分は
   未コミットのまま working tree に温存（このセッションでは触っていない）。
3. 09-02 日誌の宿題（karabiner 更新、`scripts/brewUpdate` 再実行、stash 2 件整理）は継続。

この日誌は引き継ぎ用であり、自動コミットしない。

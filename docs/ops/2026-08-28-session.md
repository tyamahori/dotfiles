# 2026-08-28 セッション

## ponytail 導入（omp / Claude Code / Codex）

[DietrichGebert/ponytail](https://github.com/DietrichGebert/ponytail) を3ホストとも
plugin-tier（モード切替・毎ターン注入・6スキル）で導入。
デフォルトモード full・subagent 注入は全 subagent（いずれも上流デフォルト、追加設定なし）。
ルール文の `agents/global-instructions.md` への転記はしない（プラグイン注入と二重になるため）。

### 完了した作業

1. **Claude Code（宣言的登録）**
   - `claude/settings.json` に `extraKnownMarketplaces.ponytail`（github: DietrichGebert/ponytail）と
     `enabledPlugins."ponytail@ponytail": true` を追加。既存の phpstorm / warp と同型。
2. **Codex（インストール済み・trust待ち）**
   - `codex plugin marketplace add DietrichGebert/ponytail` +
     `codex plugin add ponytail@ponytail` 実行済み（v4.9.0、`~/.codex/plugins/cache/ponytail/`）。
3. **omp（インストール済み・実動確認済み）**
   - `omp plugin marketplace add DietrichGebert/ponytail` +
     `omp plugin install ponytail@ponytail` 実行済み（v4.9.0、user scope）。
   - omp は `.claude-plugin/marketplace.json` fallback と `package.json` の
     `pi.extensions`（`pi-extension/index.js`）の両方を読む。
   - 新プロセス `omp -p` で検証: `PONYTAIL MODE ACTIVE — level: full` の毎ターン注入と
     6スキル（ponytail / -review / -audit / -debt / -gain / -help）の可視を確認。

### 未完・引き継ぎ（ユーザー操作が必要）

- **Claude Code**: 対話セッション（PID 49233）が起動中で、`claude` CLI が `--version` 含め
  ハングするため CLI 経由の取得は打ち切り（fail-fast）。settings.json の宣言的登録は済みなので、
  **新しい Claude Code セッションを起動 → marketplace の trust プロンプトに同意**すれば
  自動インストールされる。起動時の **statusLine セットアップの nudge は拒否**する
  （`~/.claude/settings.json` は dotfiles symlink。既に ccusage statusline 設定済みで、
  上書きされると repo が churn する）。確認は `/ponytail`（現在モード表示）。
- **Codex**: 対話セッションで `/hooks` を開き ponytail の2つのライフサイクルフックを
  **review して trust** → 新スレッド開始。確認は `/ponytail` と `@ponytail-review`。

### 運用メモ

- レベル切替: `/ponytail lite|full|ultra|off`（セッション単位）。恒久変更は
  `PONYTAIL_DEFAULT_MODE` 環境変数（現状未設定 = full）。
- アンインストール時は各ホストの remove コマンドの**前に** `node scripts/uninstall.js`
  （プラグイン checkout 内）を実行して `~/.config/ponytail/config.json` 等の残留状態を掃除する。
- 既存の Scope discipline 規範（global-instructions.md）とは思想が重なるが矛盾しない。
  正本はプラグイン側に一本化。
